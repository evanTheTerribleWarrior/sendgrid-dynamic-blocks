{
  "name": "sendgrid-dynamic-blocks",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@sendgrid/client": "^7.7.0",
    "@twilio/runtime-handler": "2.0.0",
    "form-data": "^4.0.0",
    "jsonwebtoken": "^9.0.1",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.5.2"
  },
  "engines": {
    "node": "16"
  }
}
