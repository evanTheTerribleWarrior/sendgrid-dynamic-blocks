SavedCollection:
TEST ALL SCENARIOS TO AVOID ERRORS
- Move a file/folder - SOMEHOW CANCELS THE STATE

Build block:
- Fix when deleting text fields in Settings, leaves one character. also boolean does not work correctly
- Fix errors when choosing some conditions that make it crash. perhaps exclude some conditions from top level like else if/else

Zip component:
- When uploading images, utilise better promises or even exponential backoff?

Security:
- JWT / Auth?

Update Templates
- Fetch paged templates

Future:
- web vitals to connect with segment/analytics?